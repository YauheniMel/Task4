enum Routes {
  login = '/api/login',
  register = '/api/register',
  getUsers = '/api/users',
  block = '/api/block/',
  unblock = '/api/unblock/',
  delete = '/api/del/',
  logout = '/api/logout',
}

export default Routes;
