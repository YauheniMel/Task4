import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#37474f',
      light: '#62727b'
    },
    secondary: {
      main: '#4fc3f7',
      light: '#a0e0fd'
    }
  }
});
